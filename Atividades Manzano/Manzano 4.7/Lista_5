algoritmo "Equaçao_segundo_grau"
// Função : Resolve uma equação de segundo grau
// Autor : Lucas Vinicius
// Data : 24-Sep-17
// Seção de Declarações
var

a, b, c, delta, x1, x2: real

inicio

//Mostra na tela a funcionalidade do programa
escreval ("Este programa realiza o calculo de uma equação de segundo grau")

//Entrada de dados
escreval ("Digite o valor de a: ")
   leia (a)  //Recebe o valor da primeira variável
escreval ("Digite o valor de b: ")
   leia (b)  //Recebe o valor da segunda variável
escreval ("Digite o valor de c: ")
   leia (c) //Recebe o valor da terceira variável

//Processamento e saida de dados
//Armazena o calculo de delta em uma variável
delta <- (b*b)-4*a*c
se (a=0) entao
   //Se o valor da primeira variável for igual a zero, mostrar na tela que a equação falhou
   escreval ("Equação inválida, digite um valor diferente de 0 para 'A'")
senao
     //Operação para encontrar a primeira raiz de x
   x1 <- (-b+raizq(delta))/2*a
      //Operação para encontrar a segunda raiz de x
   x2 <- (-b-raizq(delta))/2*a
      //Mostra na tela as raizes de x
   escreval ("As raizes da sua equação são: ",x1," e",x2)
fimse
fimalgoritmo
