algoritmo "Infinito_soma_media"
// Função : Lê os números digitados pelo usuário até ele não querer mais e da o valores da soma como positivo e media negativa
// Autor : Lucas Vinicius
// Data : 28-Sep-17
// Seção de Declarações
var

resposta, num, soma, mediador: inteiro

inicio

//Mostra na tela a funcionalidade do programa
escreval ("Lê os números digitados pelo usuário até ele não querer mais e da o valores da")
escreval ("soma como positivo e media como negativo")

//Laço de repetição/Entrada e processamento de dados
resposta <-1
enquanto (resposta=1) faca
   escreval ("Digite um número")
   leia (num)
   soma <- num+soma //Calcula a soma dos números
   mediador <- mediador+1 //Medidor para contar a quantidade de números lidos
   escreval ("Digite '1' para continuar ou qualquer outro número para parar")
   leia (resposta) //Recebe a reposta do usuário para continuar o loop ou não
fimenquanto

//Saida de dados
//Mostra na tela a soma dos valores digitados, tanto positivo quanto negativo
escreval ("O resultado da soma dos seus números positivos digitados é de: ",soma)
escreval ("O resultado da media dos seus números negativos digitados é de: ",(soma/mediador)*-1)

fimalgoritmo
