algoritmo "Vetor_impar"
// Função : Lê uma quantidade de números determinada pelo usuário e os ordena em posições impares
// Autor : Lucas Vinicius
// Data : 23/10/2017
//Vetor que exibe os números digitados em posições ímpares.
var

num: vetor[1..999] de real
cont: inteiro

inicio
//Mostra na tela a funcionalidade do programa
escreval ("Este programa lê uma quantidade de números determinada pelo usuário e os ordena")
escreval ("em posições impares")

//Linha para separar informações
escreval ("----------------------------------------------------------------------------------")

//Recebe a quantidade de números que usuário deseja digitar
escreval ("Digite a quantidade de numeros a serem digitados")
leia (cont)

//Laço de repetição para receber os números que o usuário deseja armazenar
para cont de 1 ate cont faca
   escreval ("Digite um numero:")
   leia (num[cont])
fimpara

//Laço para mostrar os números na ordem impar
para cont de 1 ate cont passo + 2 faca
   escreval (num[cont])
fimpara

fimalgoritmo
